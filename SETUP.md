# إعداد لوحة التحكم - دليل سريع

## 📋 قائمة المراجعة السريعة

### 1. تثبيت الحزم
```bash
cd supabase-dashboard
pnpm install
```

### 2. إعداد Supabase

قم بنسخ `.env.example` إلى `.env` وأضف بيانات مشروعك:

```bash
cp .env.example .env
```

املأ الملف بالقيم من لوحة تحكم Supabase:
```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...
```

### 3. إنشاء قاعدة البيانات

قم بتشغيل SQL Schema في Supabase SQL Editor (يجب أن يكون لديك بالفعل).

### 4. إنشاء مستخدم

في Supabase Dashboard → Authentication → Users، أضف مستخدم جديد:
- Email: admin@example.com
- Password: اختر كلمة مرور قوية

### 5. تشغيل التطبيق

```bash
pnpm dev
```

افتح المتصفح على: http://localhost:5173

## 🎯 تسجيل الدخول

استخدم البريد الإلكتروني وكلمة المرور التي أنشأتها في الخطوة 4.

## 🚀 البناء للإنتاج

```bash
pnpm build
pnpm preview
```

## 📁 هيكل المشروع

```
supabase-dashboard/
├── src/
│   ├── components/      # AppBar.vue
│   ├── views/          # Login, Courses, Locations, Exams, Users
│   ├── stores/         # auth, courses, locations, exams, users
│   ├── router/         # Vue Router + Auth Guards
│   ├── plugins/        # Vuetify RTL Configuration
│   ├── lib/           # Supabase Client
│   └── types/         # TypeScript Types
├── .env               # متغيرات البيئة (غير موجود في Git)
├── .env.example       # نموذج متغيرات البيئة
└── README.md          # التوثيق الكامل
```

## ⚠️ ملاحظات مهمة

1. **لا تنشر ملف `.env`** - يحتوي على مفاتيح سرية
2. **RLS مفعل** - تأكد من تشغيل جميع سياسات Row Level Security
3. **جميع المستخدمين مسؤولون** - كل من يسجل الدخول له صلاحيات كاملة
4. **RTL مفعل** - التطبيق كامل بالعربية من اليمين إلى اليسار

## 🐛 حل المشاكل

### خطأ في الاتصال بـ Supabase
- تأكد من صحة `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY`
- تأكد من نسخ الملف `.env.example` إلى `.env`

### لا يمكن تسجيل الدخول
- تأكد من إنشاء مستخدم في Supabase Authentication
- تأكد من تفعيل Email Authentication في Supabase

### البيانات لا تظهر
- تأكد من تطبيق SQL Schema بشكل صحيح
- تأكد من تفعيل RLS Policies

## 📞 الدعم

في حال واجهت أي مشاكل:
1. راجع ملف `README.md` للتوثيق الكامل
2. تحقق من console المتصفح لرؤية الأخطاء
3. تحقق من Supabase Dashboard → Logs

---

✅ **جاهز للاستخدام!** 🎉
