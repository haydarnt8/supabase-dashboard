[build]
  command = "pnpm install && pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.18.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
